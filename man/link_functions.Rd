% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/links.R
\name{link_functions}
\alias{link_functions}
\alias{link_logistic_normal}
\alias{link_dirichlet}
\alias{link_zero_inflated}
\title{Link functions mapping component scores to mixture proportions}
\usage{
link_logistic_normal(scores)

link_dirichlet(scores)

link_zero_inflated(scores, eps = 0.001)
}
\arguments{
\item{scores}{numeric matrix of component scores, \eqn{N \times K}.}

\item{eps}{positive sparsity threshold for the zero-inflated link
(default \code{1e-3}).}
}
\value{
numeric matrix \eqn{N \times K} of mixture proportions, with
  non-negative entries summing to one in each row.
}
\description{
These functions implement the three link specifications discussed in
Section 2.1 of Ortu and Frigau (2026): logistic-normal, Dirichlet, and
zero-inflated. Each function takes the matrix of component scores
\eqn{\boldsymbol{\Gamma}^{(C)}} (the last \eqn{K} columns of the
component-score matrix \eqn{\boldsymbol{\Gamma}}) and returns the
mixture proportions \eqn{\boldsymbol{\Theta}}.
}
\section{Functions}{
\itemize{
\item \code{link_logistic_normal()}: Logistic-normal (softmax) link, the default
specification used in the paper. Equation (3).

\item \code{link_dirichlet()}: Dirichlet link with concentration parameters
given by \eqn{\exp(\gamma_{ik})}. Equation (4). The deterministic
point estimate used in the algorithm is the normalized mean of the
Dirichlet, i.e. \code{exp(scores)} normalized rowwise; this matches
the GSCA-MM estimation routine, which works with the conditional mean
of the topic scores given the covariates.

\item \code{link_zero_inflated()}: Zero-inflated link. Sets \eqn{\theta_{ik}=0}
when \eqn{\exp(\gamma_{ik}) < \epsilon} and renormalizes the
surviving entries. Equation (5).

}}
