% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.init_phi_kmeans}
\alias{.init_phi_kmeans}
\title{k-means++ seeded initialization for the component-category matrix Phi}
\usage{
.init_phi_kmeans(W, K, subsample = 500L, iter.max = 10L)
}
\arguments{
\item{W}{non-negative count matrix \eqn{N \times V}.}

\item{K}{number of components.}

\item{subsample}{maximum number of rows used for the kmeans iterations
(default 500).}

\item{iter.max}{Lloyd iterations after seeding (default 10).}
}
\value{
numeric matrix \eqn{K \times V} on the simplex.
}
\description{
Builds a non-pathological starting Phi by k-means++ seeding the
row-normalized count matrix and refining the K centers with a few Lloyd
iterations on a subsample of rows. Reduces the local-optima frequency
of the EM-GSCA loop relative to a Dirichlet random start, especially
with K >= 5 components.
}
\details{
Cost is O(K * N_sub * V) for the seeding pass plus a constant number
of Lloyd iterations on at most \code{subsample} rows. Designed to add
negligible overhead to \code{\link{fit_gscamm}} even when called inside
a bootstrap loop.
}
\keyword{internal}
