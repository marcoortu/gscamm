% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gscamm.R
\name{gscamm_control}
\alias{gscamm_control}
\title{Control parameters for the EM-GSCA algorithm}
\usage{
gscamm_control(
  max_iter = 100,
  tol = 1e-04,
  alpha = 0.01,
  lambda0 = 1,
  rho = 0.9,
  eps = 0.001,
  delta = 0.01,
  init_Phi = NULL,
  init_B = NULL,
  min_iter = 5,
  trace_perplexity = TRUE,
  trace_every = 1,
  polish_max_iter = 20L,
  polish_tol = 1e-06
)
}
\arguments{
\item{max_iter}{maximum number of EM-GSCA iterations (default 100).}

\item{tol}{convergence tolerance on the change in
\eqn{(\boldsymbol{\Phi}, \boldsymbol{\Theta}, \boldsymbol{\Gamma})}
measured as the maximum absolute change across the three matrices
(default \code{1e-4}).}

\item{alpha}{Dirichlet smoothing constant added to the sufficient
statistics in the M-step (default \code{0.01}).}

\item{lambda0}{initial ridge parameter \eqn{\lambda_0} for the geometric
schedule \eqn{\lambda_B^{(t)} = \lambda_0 \rho^t} (default 1).}

\item{rho}{ridge decay rate \eqn{\rho \in (0, 1)} (default 0.9).}

\item{eps}{sparsity threshold used by the zero-inflated link
(default \code{1e-3}).}

\item{delta}{non-negative regularization constant added to the
responsibilities before the additive log-ratio transform in the
ALR-space GSCA update (default \code{1e-2}). Required to handle
rows of \eqn{R} that are sparse or have exact zeros (e.g. under the
zero-inflated link). Ignored when \code{gsca_space = "simplex"}.}

\item{init_Phi}{optional list with \code{Phi} (\eqn{K \times V}) for
warm-starting; default initializes from a Dirichlet on the simplex.}

\item{init_B}{optional warm-start \eqn{P \times K} matrix; default zero.}

\item{min_iter}{minimum number of iterations before convergence checks
are applied (default 5).}

\item{trace_perplexity}{if \code{TRUE}, perplexity is recomputed every
iteration and stored; otherwise only every \code{trace_every}
iterations.}

\item{trace_every}{interval at which to record perplexity when
\code{trace_perplexity = FALSE} (default 5).}

\item{polish_max_iter}{Newton iterations cap for the per-row MAP polish
(default 20). Only used when \code{fit_gscamm(polish = "map")}.}

\item{polish_tol}{max-absolute-change tolerance on \eqn{\eta} for the
MAP polish (default \code{1e-6}).}
}
\value{
a list of class \code{gscamm_control}.
}
\description{
Control parameters for the EM-GSCA algorithm
}
