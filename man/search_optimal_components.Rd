% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search_optimal_components.R
\name{search_optimal_components}
\alias{search_optimal_components}
\title{Search the optimal number of components by perplexity}
\usage{
search_optimal_components(
  W,
  X,
  Ks,
  link = "logistic_normal",
  control = gscamm_control(),
  holdout = 0,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{W}{count matrix.}

\item{X}{covariate matrix or data frame.}

\item{Ks}{integer vector of candidate component counts.}

\item{link}{see \code{\link{fit_gscamm}}.}

\item{control}{see \code{\link{gscamm_control}}.}

\item{holdout}{fraction of token mass held out per row for held-out
perplexity (default 0, meaning in-sample only).}

\item{seed}{optional integer for reproducibility (governs the holdout
sampling and the model initialization).}

\item{verbose}{logical.}
}
\value{
data frame with columns \code{K}, \code{perplexity}, and
  optionally \code{holdout_perplexity}.
}
\description{
Fits the GSCA-MM model for each \eqn{K} in a user-provided grid and
reports perplexity (in-sample, and optionally held-out via random
sub-sampling of the count matrix). The user can then pick the \eqn{K}
that minimizes (or stabilizes) the chosen criterion.
}
