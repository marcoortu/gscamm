% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map_polish.R
\name{.estimate_sigma2_alr}
\alias{.estimate_sigma2_alr}
\title{Estimate per-component prior variance from ALR residuals of responsibilities}
\usage{
.estimate_sigma2_alr(
  R,
  X_std,
  B_minus,
  ref,
  delta = 0.01,
  P_eff = ncol(X_std),
  floor = 1e-08
)
}
\arguments{
\item{R}{responsibilities matrix \eqn{N \times K}.}

\item{X_std}{standardized covariates \eqn{N \times P}.}

\item{B_minus}{structural coefficients \eqn{P \times (K-1)} (non-reference).}

\item{ref}{reference component index.}

\item{delta}{smoothing constant for ALR (matches control$delta).}

\item{P_eff}{effective number of estimated covariates (defaults to ncol(X_std)).}

\item{floor}{minimum allowed variance (default 1e-8).}
}
\value{
numeric vector of length \eqn{K-1}.
}
\description{
Given the converged responsibilities \eqn{R} and the structural mean
\eqn{X_i^\top \widehat{B}_{-K}}, computes
\deqn{\widehat\sigma^2_k = \frac{1}{N - P} \sum_i \big(\mathrm{alr}_k(\tilde R_i) - X_i^\top \widehat{B}_{-K,k}\big)^2,}
where \eqn{\tilde R_i} is \code{R} after \code{delta}-smoothing and ALR
is taken with reference \code{ref}. A small floor avoids degeneracies.
}
\keyword{internal}
